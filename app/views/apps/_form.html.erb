<%= simple_form_for(@app) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :version %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
